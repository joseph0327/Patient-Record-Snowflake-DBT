-- ############################## BRONZE##############################

USE DATABASE PATIENT_RECORD_DB;
USE SCHEMA BRONZE;

ALTER TABLE tablename
RENAME TO newtablename;

show tables;

-- 1. RAW_PATIENT(STREAM DATA)
CREATE OR REPLACE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_PATIENT
(
    PATIENT_ID            STRING,
    FIRST_NAME            STRING,
    LAST_NAME             STRING,
    DATE_OF_BIRTH         DATE,
    GENDER                STRING,
    SSN                   STRING,
    EMAIL                 STRING,
    PHONE                 STRING,
    ADDRESS               STRING,
    INSURANCE_NUMBER      STRING,
    INSURANCE_COMPANY     STRING,
    MEDICAL_HISTORY       ARRAY,
    CREATED_TIMESTAMP     TIMESTAMP_NTZ,

    -- Ingestion metadata
    SEQUENCE              NUMBER,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ
);

SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_PATIENT;
TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_PATIENT;




-- 2. RAW_VISIT (STREAM DATA)
CREATE OR REPLACE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_VISIT
(
    VISIT_ID              STRING,
    PATIENT_ID            STRING,
    APPOINTMENT_DATE      DATE,
    DOCTOR_ID             STRING,
    DEPARTMENT            STRING,
    DIAGNOSIS             STRING,
    VISIT_REASON          STRING,
    VISIT_STATUS          STRING,
    CREATED_TIMESTAMP     TIMESTAMP_NTZ,
    
     -- Ingestion metadata
    SEQUENCE              NUMBER,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ
);
 
SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_VISIT;
TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_VISIT;


-- 3. RAW_DIAGNOSIS (JSON)
CREATE OR REPLACE TABLE BRONZE_DIAGNOSIS
(
    DIAGNOSIS_ID          STRING,
    PATIENT_ID            STRING,
    VISIT_ID              STRING,
    ICD10_CODE            STRING,
    DIAGNOSIS_NAME        STRING,
    SEVERITY              STRING,
    DIAGNOSIS_DATE        DATE,
    DOCTOR_ID             STRING,
    CREATED_TIMESTAMP     TIMESTAMP_NTZ,

    SEQUENCE              NUMBER AUTOINCREMENT,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ 
);

SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_DIAGNOSIS
TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_DIAGNOSIS

-- 4. RAW_MEDICATION (STREAM DATA)
CREATE OR REPLACE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_MEDICATION
(
    MEDICATION_ID         STRING,
    PATIENT_ID            STRING,
    MEDICATION_NAME       STRING,
    DOSAGE                STRING,
    FREQUENCY             STRING,
    PRESCRIBED_BY         STRING,
    START_DATE            DATE,
    STATUS                STRING,
    CREATED_TIMESTAMP     TIMESTAMP_NTZ,

     -- Ingestion metadata
    SEQUENCE              NUMBER,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ
);

SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_MEDICATION;

TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_MEDICATION;

-- 5. RAW_LAB_RESULT(CSV)
CREATE OR REPLACE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_LAB_RESULT
(
    LAB_ID                STRING,
    PATIENT_ID            STRING,
    LAB_CATEGORY          STRING,
    TEST_NAME             STRING,
    RESULT_VALUE          STRING,
    UNIT                  STRING,
    REFERENCE_RANGE       STRING,
    RESULT_STATUS         STRING,
    LAB_LOCATION          STRING,
    TEST_DATE             DATE,
    ORDERING_DOCTOR_ID    STRING,

    CREATED_TIMESTAMP     TIMESTAMP_NTZ,

    SEQUENCE              NUMBER AUTOINCREMENT,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ
);

SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_LAB_RESULT;
TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_LAB_RESULT;



-- 6. RAW_BILLING (STREAM DATA)
CREATE OR REPLACE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_BILLING
(
    BILLING_ID            STRING,
    PATIENT_ID            STRING,
    INSURANCE_NUMBER      STRING,
    CLAIM_NUMBER          STRING,
    SERVICE_TYPE          STRING,
    AMOUNT                NUMBER(10,2),
    BILLING_STATUS        STRING,
    BILLING_DATE          TIMESTAMP_NTZ,
    CREATED_TIMESTAMP     TIMESTAMP_NTZ,

     -- Ingestion metadata
    SEQUENCE              NUMBER,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ
);


SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_BILLING;
TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_BILLING;



--  7. RAW_INSURANCE (STREAM DATA)
CREATE OR REPLACE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_INSURANCE
(
    INSURANCE_ID          STRING,
    PATIENT_ID            STRING,
    INSURANCE_NUMBER      STRING,
    INSURANCE_COMPANY     STRING,
    PLAN_NAME             STRING,
    COVERAGE_AMOUNT       NUMBER(12,2),
    EFFECTIVE_DATE        DATE,
    EXPIRATION_DATE       DATE,
    STATUS                STRING,
    CREATED_TIMESTAMP     TIMESTAMP_NTZ,

    -- Ingestion metadata
    SEQUENCE              NUMBER,
    SOURCE_SYSTEM         STRING,
    INGESTION_TIMESTAMP   TIMESTAMP_NTZ
    
);

SELECT * FROM PATIENT_RECORD_DB.BRONZE.BRONZE_INSURANCE;
TRUNCATE TABLE PATIENT_RECORD_DB.BRONZE.BRONZE_INSURANCE;

